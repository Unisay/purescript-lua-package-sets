# Release artifacts assembled from src/packages.dhall at release time
# (scripts/gen-package-set-json / the release workflow). The single source of
# truth is src/packages.dhall; these are generated, never committed.
/packages.dhall
/packages.json

# Local-only working files (drafts, notes); never pushed.
/.local/
